# <Project Name>

## Purpose

<What this project is, in a few sentences. The PRIME "P" — what outcome are
we actually after?>

## Context

<Domain background, constraints, people involved, anything a fresh session
needs to not re-ask basic questions. Update this as it evolves — it's not
frozen at creation.>

## Current state

<One paragraph: where things stand right now. This is the first thing to
read when picking the project back up.>

## Environment

<What has to be true on a machine for this project to actually be workable.
Empty/"none yet" is fine for a project that hasn't needed any tooling yet —
don't invent requirements before they're real. Fill this in as soon as the
project needs anything specific, and keep it accurate: this is what a fresh
clone on a new machine runs before doing any real work (see root
`CLAUDE.md`, "The first move" step 4).>

**Requirements** — tools/versions/accounts this project needs.

**Verify** — command(s) to check the above is actually in place:
```bash
<e.g. `which some-tool && some-tool --version`>
```

**Setup** — what to run if Verify fails:
```bash
<e.g. install/build/config steps>
```

## Work Log

### <YYYY-MM-DD> — <short title>
- What was discussed/decided/built.
- Anything that surprised us or changed direction.
- What's next.
